import * as React from 'react';
import MuiFade from '@material-ui/core/Fade';

/** ============================ Components ================================ */
export const Fade = (props) => <MuiFade {...props} />;
